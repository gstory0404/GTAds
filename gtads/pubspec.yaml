name: gtads
description: Aggregate ad plugin
version: 0.0.5
homepage: https://github.com/gstory0404/GTAds

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
#  flutter_lints: ^2.0.0

flutter:
  plugin:
    platforms:
      android:
        package: com.gstory.gtads
        pluginClass: GtadsPlugin
      ios:
        pluginClass: GtadsPlugin